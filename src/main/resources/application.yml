jikanapi:
  url:https://api.jikan.moe/v3

server:
  servlet:
    context-path: /myapp

spring:
  main:
    banner-mode: "off"
datasource:
  platform: h2

logging:
  level:
    org:
      springframework: ERROR